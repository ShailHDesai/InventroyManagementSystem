// Define a class named Customer to represent customer-related data.
public class Customer
{
    // This code line will declare a CustomerId property of type string and initialize it with an empty string to ensure it is never null.
    public string CustomerId { get; set; } = string.Empty; // or some default value
    // This code line will declare a CompanyName property of type string and initialize it with an empty string to prevent null values.
    public string CompanyName { get; set; } = string.Empty;
    // This code line will declare a ContactName property of type string and initialize it with an empty string to prevent null values.
    public string ContactName { get; set; } = string.Empty;
    // This code line will declare a ContactTitle property of type string and initialize it with an empty string to prevent null values.
    public string ContactTitle { get; set; } = string.Empty;
    // This code line will declare a City property of type string and initialize it with an empty string to prevent null values.
    public string City { get; set; } = string.Empty;
    // This code line will declare an Address property of type string and initialize it with an empty string to prevent null values.
    public string Address { get; set; } = string.Empty;
}
